import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Minimal config for OpenNext on Cloudflare.
// You can add overrides (e.g., R2 incremental cache) later if needed.
export default defineCloudflareConfig({});
